const gameBoard = function (){
    const row = 3;
    const column = 3;
    let board = [];

    for(let i = 0; i<row;i++){
        board[i] = [];
        for(let j = 0; j<column;j++){
            board.push(" ");
        }
    }

    const displayBoard = () => {
         
    }
}